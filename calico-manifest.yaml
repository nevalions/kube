apiVersion: operator.tigera.io/v1
kind: Installation
metadata:
  name: default
spec:
  calicoNetwork:
    ipPools:
      - cidr: 10.244.0.0/16
        blockSize: 26
        encapsulation: None
        natOutgoing: Enabled
        nodeSelector: all()
