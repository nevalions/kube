apiVersion: v1
kind: Service
metadata:
  name: nginx-http
  namespace: default
spec:
  type: NodePort
  selector:
    app: nginx-http
  ports:
    - port: 80
      targetPort: 80
  externalTrafficPolicy: Cluster